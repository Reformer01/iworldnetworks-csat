{
  "entities": {
    "Feedback": {
      "title": "Feedback",
      "description": "Customer feedback entry from the public portal",
      "type": "object",
      "properties": {
        "customerName": { "type": "string" },
        "customerEmail": { "type": "string", "format": "email" },
        "category": { "type": "string", "enum": ["Reliability", "Support", "Testimonials", "Installation", "Billing"] },
        "servicePlan": { "type": "string" },
        "location": { "type": "string", "enum": ["Abeokuta", "Ibadan", "Osogbo", "Akure"] },
        "ratings": { 
          "type": "object",
          "properties": {
            "stability": { "type": "number" },
            "journey": { "type": "number" },
            "professionalism": { "type": "number" },
            "clarity": { "type": "number" },
            "signal": { "type": "number" },
            "punctuality": { "type": "number" },
            "quality": { "type": "number" },
            "accuracy": { "type": "number" },
            "reconnection": { "type": "number" }
          }
        },
        "comment": { "type": "string" },
        "staffName": { "type": "string" },
        "timestamp": { "type": "number" },
        "status": { "type": "string", "enum": ["pending", "reviewed", "escalated"], "default": "pending" },
        "interviewConsent": { "type": "string", "enum": ["Yes", "No", "Maybe"] }
      },
      "required": ["customerEmail", "category", "location", "timestamp"]
    },
    "Staff": {
      "title": "Staff",
      "description": "Internal support staff member",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "department": { "type": "string", "default": "Customer Support" }
      },
      "required": ["name"]
    },
    "Technician": {
      "title": "Technician",
      "description": "Field technician profile with territory details",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "partner": { "type": "string" },
        "region": { "type": "string", "enum": ["Abeokuta", "Ibadan", "Osogbo", "Akure"] },
        "primaryTerritory": { "type": "string" }
      },
      "required": ["name", "region"]
    }
  },
  "auth": {
    "providers": ["google.com", "password"]
  },
  "firestore": {
    "/feedbacks/{feedbackId}": {
      "schema": "Feedback",
      "description": "Collection of all customer feedback"
    },
    "/staff/{staffId}": {
      "schema": "Staff",
      "description": "Registry of support staff"
    },
    "/technicians/{techId}": {
      "schema": "Technician",
      "description": "Registry of field technicians"
    }
  }
}